- name: Upload Branded Images
        uses: actions/upload-artifact@v4
        with:
          name: KushFC-Final-Posts
          path: "*.jpg" # זה יתפוס את כל הפוסטים של כל הליגות
          if-no-files-found: ignore
